import React from 'react';
import {storiesOf} from '@storybook/react-native';
import HeaderWrapper from '../../../src/components/HeaderWrapper';

storiesOf('Header Wrapper', module).add('Default', () => <HeaderWrapper />);
